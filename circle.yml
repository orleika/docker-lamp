machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt-get update
    - sudo apt-get install docker-engine
    - sudo pip install docker-compose

test:
  override:
    - docker-compose up -d
    - ./test.sh
